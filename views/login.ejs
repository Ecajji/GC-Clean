<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login - GC Clean</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #43a047, #81c784);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Poppins', sans-serif;
    }
    .card {
      width: 400px;
      border: none;
      border-radius: 20px;
      box-shadow: 0 8px 18px rgba(0,0,0,0.2);
    }
    .btn-success {
      width: 100%;
      border-radius: 50px;
    }
    .form-control {
      border-radius: 10px;
    }
    .logo {
      font-weight: 700;
      font-size: 1.6rem;
      color: #2e7d32;
    }
  </style>
</head>
<body>
  <div class="card p-4 bg-white">
    <h3 class="text-center logo mb-3">GC Clean</h3>
    <h5 class="text-center text-muted mb-4">Welcome Back</h5>

    <form action="/login" method="POST">
      <div class="mb-3">
        <label class="form-label">School Email</label>
        <input type="email" name="email" value="<%= values?.email || '' %>"
               class="form-control <%= errors?.email ? 'is-invalid' : '' %>">
        <% if (errors?.email) { %><div class="invalid-feedback"><%= errors.email %></div><% } %>
      </div>

      <div class="mb-3">
        <label class="form-label">Password</label>
        <input type="password" name="password"
               class="form-control <%= errors?.password ? 'is-invalid' : '' %>">
        <% if (errors?.password) { %><div class="invalid-feedback"><%= errors.password %></div><% } %>
      </div>

      <button type="submit" class="btn btn-success mt-3">Login</button>
      <p class="text-center mt-3 mb-0">Don’t have an account?
        <a href="/register" class="text-success fw-semibold">Register</a>
      </p>
    </form>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
